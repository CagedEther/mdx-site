# mdx-site

  MDX-powered React landing page — SolarCalc solar savings calculator.

  Built with React + Vite + Tailwind CSS + shadcn/ui + MDX.

  Content is driven by `src/content/data.ts` (structured data) and `src/content/*.mdx` (rich prose).
  